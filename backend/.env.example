# MovieSir Backend 환경 변수 예시
# 이 파일을 복사해서 .env로 사용하세요

# 서버 설정
HOST=0.0.0.0
PORT=8000

# CORS 허용 Origin (쉼표로 구분)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 추후 DB 연결 시 사용
# DATABASE_URL=postgresql://user:password@localhost:5432/moviesir

# 추후 AI 연결 시 사용
# OPENAI_API_KEY=sk-xxx
